name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build macOS arm64
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Show toolchain
        run: |
          sw_vers
          xcodebuild -version
          swiftc --version
          cat VERSION

      - name: Build (no install)
        env:
          AXON_SKIP_INSTALL: "1"
        run: bash Sources/build.sh

      - name: Smoke-check bundle
        run: |
          APP=".build/ClaudeBar.app"
          test -x "$APP/Contents/MacOS/ClaudeBar"
          test -x "$APP/Contents/PlugIns/ClaudeBarWidget.appex/Contents/MacOS/ClaudeBarWidget"
          /usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "$APP/Contents/Info.plist"
          codesign --verify --deep --strict "$APP"
          echo "bundle OK"
