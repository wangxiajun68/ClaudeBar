# Build artifacts
.build/
build/

# macOS
.DS_Store
*.swp
*~

# Xcode (not used, but ignore if opened)
*.xcodeproj/
*.xcworkspace/
DerivedData/

# Claude Code local settings
.claude/
.claude/worktrees/

# Design source (binary, edited via Pencil)
*.pen

# Stray env-expansion artifacts (e.g. a literal "{{HOMEBREW_PREFIX}}/" dir)
{{*/
