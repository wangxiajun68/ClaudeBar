# Build artifacts
.build/
build/

# macOS
.DS_Store
*.swp
*~

# Xcode (not used, but ignore if opened)
*.xcodeproj/
*.xcworkspace/
DerivedData/

# Claude Code / Codex local settings
.claude/
.claude/worktrees/
.codex/

# Design source (binary, edited via Pencil)
*.pen

# Accidental screenshots dropped at repo root
/screenshot-*.png

# Stray env-expansion leftover (literal directory named {{HOMEBREW_PREFIX}})
/[{{][{]HOMEBREW_PREFIX[}][}]/
